# pingMyDomain
This Python script is designed for use with a Raspberry Pi to periodically ping a specified URL to check its status. It sends a GET request to the URL every 120 seconds and logs the response status code. If there is an error, such as a connectivity issue, it catches the exception and prints the error message.
